# Socratic Companion — 逻辑流程详解

> 一句话概括：**学生发消息进来 → 了解他 → 判断他 → 引导他 → 记录他**。

---

## 整体流程图

```
Student Message（经 graph.py 路由，event_type == "student_message"）
      │
      ▼
┌──────────────────────────────────────────────────────────────────────┐
│  阶段 1：加载上下文（"我先了解这个学生"）                              │
│                                                                      │
│  ① student_cognitive_models  ← 该学生的认知快照（掌握/误解/偏好）      │
│  ② teacher_authority_graph   ← Architect 写入的知识边界与课程范围      │
│  ③ interaction_episodes      ← 该学生历史交互（问过什么、错过什么）     │
│                                                                      │
│  三者组成 Working Memory，传给 LLM                                    │
└──────────────────────────────────────────────────────────────────────┘
      │
      ▼
┌──────────────────────────────────────────────────────────────────────┐
│  阶段 2：LLM ReAct 推理（"我判断怎么引导他"）                         │
│                                                                      │
│  输入：System Prompt + Working Memory + 学生当前消息                  │
│  输出：结构化推理 ↓                                                   │
│                                                                      │
│  Observation:       学生说了什么                                      │
│  Pattern Analysis:  与历史错误模式匹配                                 │
│  Strategy Selection:为什么选这种引导方式                               │
│  Expected Student Action: 学生接下来应该做什么                         │
└──────────────────────────────────────────────────────────────────────┘
      │
      ├─── 正常引导 ─────────────────────┐
      │                                   │
      ├─── 学生崩溃/反复失败 ─────────┐   │
      │                               │   │
      ├─── 问题超出课程范围 ──────┐   │   │
      │                           ▼   ▼   ▼
      │                    ┌──────────────────────────────────────┐
      │                    │  阶段 3：执行工具（"生成引导内容"）     │
      │                    │                                      │
      │                    │  路径 A：construct_hint()             │
      │                    │    → 选策略、生成 hint、附带追问      │
      │                    │                                      │
      │                    │  路径 B：escalate_to_human()          │
      │                    │    → 记录日志、返回安慰消息            │
      │                    │                                      │
      │                    │  路径 C：知识边界拦截                  │
      │                    │    → bridge / decline 引导回复         │
      │                    └──────────────────────────────────────┘
      │                                   │
      │                                   ▼
      │                    ┌──────────────────────────────────────┐
      │                    │  阶段 4：更新认知模型（"记住他的表现"） │
      │                    │                                      │
      │                    │  update_student_cognition_map()       │
      │                    │  → 更新置信度、记录误解、调整偏好      │
      │                    └──────────────────────────────────────┘
      │                                   │
      │                                   ▼
      │                    ┌──────────────────────────────────────┐
      │                    │  阶段 5：输出与持久化                  │
      │                    │                                      │
      │                    │  ① state["response_to_student"] ← 回复│
      │                    │  ② interaction_episodes ← 存档记录    │
      │                    │  ③ 返回 state → graph 路由（通常 END） │
      │                    └──────────────────────────────────────┘
```

---

## 阶段详解

### 阶段 1：加载上下文

节点启动后的**第一件事**是从记忆系统加载三块信息：

| 数据源 | 存储位置 | 内容 | 来源 |
|--------|----------|------|------|
| 学生认知模型 | `memory/external` → `student_cognitive_models` | 各概念的置信度、误解模式、学习风格偏好 | Companion 自己每次交互后写入 |
| 知识边界 | `memory/shared` → `teacher_authority_graph` | 课程范围、知识节点、有效性约束、scope_level | Architect 上传教材时写入 |
| 历史交互 | `memory/external` → `interaction_episodes` | 过去的对话记录、工具调用、推理链 | Companion 每次交互后写入 |

**特殊情况**：如果是新学生（无历史认知模型），初始化默认模型——所有概念 `uncertainty = 1.0`（完全未知）。

---

### 阶段 2：LLM ReAct 推理

将 System Prompt + Working Memory + 学生当前输入传给 LLM，LLM 按 ReAct 格式输出推理过程，然后决定调用哪个工具。

**推理示例**：

```
Observation:  学生说"力不就是质量乘速度吗？"
Pattern Analysis:  历史记录显示他已经 2 次把动量和力混淆，前 2 次用了 socratic 策略未纠正
Strategy Selection: socratic 已失效 → 切换到 confront（展示矛盾让他自我纠正）
Expected Student Action: 学生应意识到 F=ma 与 p=mv 的区别
```

**推理结果 → 三条路径**：

| 判断 | 下一步 | 触发条件 |
|------|--------|----------|
| 正常引导 | 调用 `construct_hint()` | 大多数情况 |
| 学生需要人工帮助 | 调用 `escalate_to_human()` | 同概念 ≥5 次错误，或检测到挫败/情绪异常 |
| 问题超出课程范围 | 不调工具，直接用知识边界生成 bridge/decline 回复 | `scope_level` 判定为超范围 |

---

### 阶段 3：执行工具

#### 路径 A：`construct_hint()`（最常见）

根据错误类型和学生画像，选择一种 hint 策略：

| 策略 | 适用场景 | 示例 |
|------|----------|------|
| **socratic** | 浅层理解错误，追问即可引导 | "你觉得力的单位是什么？如果力=质量×速度，单位对得上吗？" |
| **analogy** | 概念迁移困难，需要熟悉的类比 | "想象你推一辆购物车，推得越快需要越大的力……" |
| **decompose** | 复杂问题卡住，需要拆步骤 | "我们先搞清楚什么是力，再看什么是动量，最后比较" |
| **confront** | 顽固误解，需要展示矛盾 | "如果 F=mv，那静止物体的力就是 0，可你站在秤上为什么有读数？" |

**策略切换规则**：

```
同概念连续错误 ≥ 3 次 且策略未变 → 强制切换到另一种策略
同概念连续错误 ≥ 5 次 → 放弃引导，触发 escalate_to_human
```

#### 路径 B：`escalate_to_human()`

| 触发原因 | urgency | student_message 示例 |
|----------|---------|---------------------|
| `frustration` | medium | "我理解你现在有点困惑，让我帮你联系老师来一起看看" |
| `repeated_failure` | high | "这个问题确实有难度，我已经通知老师来帮助你了" |
| `out_of_scope` | low | "这个问题超出了当前课程，我帮你记下来让老师回头解答" |
| `emotional_distress` | high | "没关系，学习就是一步步来的，老师很快会来和你聊聊" |

MVP 阶段实现为日志记录 + 安慰消息返回，无真实推送通道。

#### 路径 C：知识边界拦截

根据 `teacher_authority_graph` 中的 `scope_level` 决定回复方式：

| scope_level | 回复方式 | 示例 |
|-------------|----------|------|
| `strict` | **decline** — 明确告知不在范围内 | "这个问题不在我们当前课程范围，我们先集中精力把力学搞懂" |
| `moderate` | **bridge** — 桥接到课程相关内容 | "有趣的问题！它其实和我们正在学的 XX 概念有关联……" |
| `permissive` | 正常引导 | 按 construct_hint 流程处理 |

---

### 阶段 4：更新认知模型

**每次交互都必须执行**，无论学生答对答错。

调用 `update_student_cognition_map`，输入本次交互数据：

```python
interaction_data = {
    "concept": "牛顿第二定律",      # 本次涉及的概念
    "student_response": "力=质量×速度",  # 学生的回答
    "is_correct": False,             # 是否正确
    "time_spent": 45.2,              # 本轮花了多少秒
    "help_requests": 1               # 本轮请求了几次帮助
}
```

**置信度更新规则**：

| 情况 | 置信度变化 | 附加动作 |
|------|-----------|----------|
| 答对且快速 | 大幅提升（如 +0.2） | 无 |
| 答对但慢/多次求助 | 小幅提升（如 +0.05） | 标记"理解不牢固" |
| 答错 | 降低（如 -0.15） | 记录误解模式到 `new_misconceptions` |

更新结果写入两处：
- `student_cognitive_models`（External Memory）—— 供下次交互阶段 1 读取
- `cognition_snapshots`（Archive / PostgreSQL）—— 供长期趋势分析

---

### 阶段 5：输出与持久化

1. **回复学生**：将 hint 内容 + follow_up_questions 组装为自然语言回复，写入 `state["response_to_student"]`
2. **存档交互**：将完整交互记录（输入、输出、工具调用链、推理过程）写入 `interaction_episodes`
3. **返回 state**：graph 根据 `agent_decision` 路由，绝大多数情况走向 `END`（把回复给学生）

---

## 完整示例

> 学生第 3 次问："力不就是质量乘速度吗？"

| 阶段 | 具体动作 |
|------|----------|
| **1. 加载** | 读取认知模型：`"力与动量"` 概念 `uncertainty=0.9`；读取历史：前 2 次用了 socratic 策略但均未纠正 |
| **2. 推理** | LLM 判断：socratic 已用 2 次无效 → 决定切换到 **confront** |
| **3. 执行** | `construct_hint(strategy="confront")` → 生成："如果 F=mv，那静止物体的力就是 0，可你站在秤上为什么有读数？" |
| **4. 更新** | `update_student_cognition_map(is_correct=False)` → 置信度下降，记录误解 `"confuses_force_and_momentum"` |
| **5. 输出** | 回复学生矛盾式引导；存档交互记录 |

> 如果继续错到**第 5 次**：

| 阶段 | 变化 |
|------|------|
| **2. 推理** | 连续错误 ≥5 → 决定 escalate |
| **3. 执行** | `escalate_to_human(reason="repeated_failure", urgency="high")` → 返回安慰消息 |
| **5. 输出** | "这个问题确实有难度，我已经通知老师来帮助你了" |

---

## 铁律总结

1. **永远不给直接答案** —— 只通过提问、类比、分解、展示矛盾来引导。
2. **不问兴趣/爱好** —— 对话严格围绕当前主题或习题；兴趣推断由 Catalyst 做。
3. **每次交互必须更新认知模型** —— 即使学生没有回答问题。
4. **策略必须自适应** —— ≥3 次同策略失败必须切换；≥5 次失败必须 escalate。
5. **尊重知识边界** —— 每次交互前加载 Architect 的最新边界，不引导超出课程范围。
